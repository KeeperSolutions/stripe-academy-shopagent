"""Routers for the commerce API (D6, D8).

Empty until step 3. Every router defined here is mounted in `api/main.py` with
`dependencies=[Depends(require_api_key)]`, which is where authentication comes
from — a route file never repeats it, and a route added to a mounted router is
protected by that fact alone.
"""
